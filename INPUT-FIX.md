# ✅ Финальные исправления - Ввод чисел

## Дата: 16 мая 2026 (18:32)

### 🐛 Проблема
1. При стирании значения в поле ввода автоматически подставлялось fallback значение (100)
2. Невозможно было ввести промежуточные значения (например, 30)
3. Лимит 1,000,000 был избыточным для большинства экспериментов

### ✅ Решение

#### 1. Исправлена логика ввода
**Было:**
```javascript
onChange={e => setCount(Math.max(1, Math.min(1000000, +e.target.value || 100)))}
```
- При пустом поле `+e.target.value` давал `0`, затем `|| 100` подставлял 100

**Стало:**
```javascript
onChange={e => {
  const val = +e.target.value;
  if (e.target.value === '' || isNaN(val)) return;
  setCount(Math.max(1, Math.min(100000, val)));
}}
```
- Проверяем пустое поле и NaN
- Если поле пустое - ничего не делаем (return)
- Пользователь может спокойно стереть и ввести новое значение

#### 2. Уменьшен лимит до 100,000
- Более разумный предел для веб-приложения
- Достаточно для серьезных статистических экспериментов
- Не перегружает браузер

### 📊 Обновленные эксперименты

| Эксперимент | Поле | Лимит | Мин |
|-------------|------|-------|-----|
| 🪙 Монетка | Бросков за раз | 100,000 | 1 |
| 🎲 Кубик | Бросков за раз | 100,000 | 1 |
| 🎂 Дни рождения | Симуляций на точку | 100,000 | 100 |
| 🏫 Зануда | Число симуляций | 100,000 | 1 |
| 📝 ЕГЭ | Симуляций | 100,000 | 100 |
| 🚪 Монти Холл | Симуляций | 100,000 | 100 |
| 🧪 χ²-тест | Число бросков | 100,000 | 30 |
| 🔔 Нормальное | Число бросков | 100,000 | 50 |

### 🎯 Результат

✅ Можно вводить любые числа в пределах лимита  
✅ Можно стирать и вводить заново  
✅ Нет автоподстановки при пустом поле  
✅ Валидация работает корректно  
✅ Производительность оптимальна  

### 🚀 Сборка

```
CSS: 21.55 kB (4.96 kB gzip)
JS: 423.12 kB (140.03 kB gzip)
Время: 625ms
```

---

**Все проблемы решены!** ✅
